# # Local PostgreSQL for GrowthOS backend (signup, users, Meta ads data).
# # From repo root: docker compose up -d
# # Then: cd backend && npm run db:push

# services:
#   postgres:
#     image: postgres:16-alpine
#     container_name: growthos-postgres
#     restart: unless-stopped
#     ports:
#       - '5432:5432'
#     environment:
#       POSTGRES_USER: postgres
#       POSTGRES_PASSWORD: postgres
#       POSTGRES_DB: Automation_Meta_Ads
#     volumes:
#       - growthos_pg_data:/var/lib/postgresql/data
#     healthcheck:
#       test: ['CMD-SHELL', 'pg_isready -U postgres -d Automation_Meta_Ads']
#       interval: 3s
#       timeout: 5s
#       retries: 10

# volumes:
#   growthos_pg_data:
